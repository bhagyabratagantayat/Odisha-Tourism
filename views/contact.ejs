<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Contact & Booking - Odisha Tourism</title>
  <link rel="stylesheet" href="/css/style.css">
  <script src="/js/contact.js" defer></script>
  <link rel="icon" type="image/png" href="https://iili.io/KYTyEDg.png">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">


  <style>
    :root {
      --peach-light: #FFECC0;
      --peach: #FFC29B;
      --rose: #F39F9F;
      --maroon: #B95E82;
      --text-dark: #333;
      --muted: #555;
      --deep-forest-green:#043927;
      
    }

    body {
      font-family: 'Poppins', sans-serif;
      background: var(--peach-light);
      color: var(--text-dark);
      margin: 0;
      line-height: 1.6;
    }

    /* Navbar */
    .navbar {
      background: var(--deep-forest-green);
      padding: 14px 25px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      position: sticky;
      top: 0;
      z-index: 1000;
    }
    .navbar .logo {
      display: flex;
      align-items: center;
      font-weight: bold;
      font-size: 18px;
      gap: 10px;
      color: white;
    }
    .navbar img {
      height: 40px;
      border-radius: 50%;
    }
    .nav-links {
      list-style: none;
      display: flex;
      gap: 18px;
    }
    .nav-links a {
      text-decoration: none;
      color: white;
      font-weight: 500;
      transition: 0.3s;
    }
    .nav-links a:hover,
    .nav-links a.active {
      color: var(--peach-light);
    }
    .hamburger {
      display: none;
      font-size: 24px;
      color: white;
      cursor: pointer;
    }

    /* Contact Section */
    .contact-section {
      display: grid;
      grid-template-columns: 2fr 1fr;
      gap: 30px;
      max-width: 1100px;
      margin: 50px auto;
      padding: 0 20px;
    }
    .contact-card {
      background: #fff;
      padding: 30px;
      border-radius: 16px;
      border-left: 6px solid var(--maroon);
      box-shadow: 0 6px 18px rgba(0,0,0,0.1);
    }
    .contact-card h2 {
      color: var(--maroon);
      margin-bottom: 10px;
    }
    .muted {
      color: var(--muted);
      margin-bottom: 20px;
    }
    .contact-form {
      display: grid;
      gap: 16px;
    }
    .contact-form .row {
      display: flex;
      gap: 16px;
      flex-wrap: wrap;
    }
    .field {
      flex: 1;
      display: flex;
      flex-direction: column;
    }
    .field label {
      font-size: 14px;
      margin-bottom: 5px;
      font-weight: 600;
      color: var(--rose);
    }
    input, select, textarea {
      padding: 12px 14px;
      border: 1px solid var(--rose);
      border-radius: 10px;
      font-size: 14px;
      transition: 0.3s;
    }
    input:focus, select:focus, textarea:focus {
      border-color: var(--maroon);
      box-shadow: 0 0 6px rgba(185,94,130,0.4);
      outline: none;
    }
    .actions {
      display: flex;
      gap: 12px;
    }
    .btn {
      flex: 1;
      padding: 12px;
      border: none;
      border-radius: 10px;
      font-weight: bold;
      cursor: pointer;
      transition: 0.3s;
    }
    .btn.primary {
      background: var(--rose);
      color: #fff;
    }
    .btn.primary:hover {
      background: var(--maroon);
    }
    .btn.ghost {
      background: var(--peach);
      color: var(--text-dark);
    }
    .btn.ghost:hover {
      background: var(--rose);
      color: #fff;
    }

    .contact-info {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }
    .info-block {
      background: #fff;
      padding: 20px;
      border-radius: 14px;
      border-left: 6px solid var(--rose);
      box-shadow: 0 4px 14px rgba(0,0,0,0.08);
    }
    .info-block h3 {
      margin: 0 0 6px;
      color: var(--maroon);
    }

    /* Booking Section */
    .booking-section {
      max-width: 1100px;
      margin: 40px auto 60px;
      padding: 30px;
      background: #fff;
      border-radius: 16px;
      border-top: 6px solid var(--rose);
      box-shadow: 0 8px 20px rgba(0,0,0,0.12);
    }
    .booking-section h2 {
      text-align: center;
      color: var(--maroon);
      margin-bottom: 8px;
    }
    .booking-section p {
      text-align: center;
      margin-bottom: 25px;
      color: var(--muted);
    }
    .booking-form {
      display: grid;
      gap: 18px;
    }
    .booking-form .row {
      display: flex;
      gap: 16px;
      flex-wrap: wrap;
    }
    .booking-form button {
      padding: 14px;
      border: none;
      border-radius: 10px;
      background: var(--rose);
      color: #fff;
      font-size: 16px;
      font-weight: bold;
      cursor: pointer;
      transition: 0.3s;
    }
    .booking-form button:hover {
      background: var(--maroon);
      transform: translateY(-2px);
    }

    /* Responsive */
    @media (max-width: 900px) {
      .contact-section {
        grid-template-columns: 1fr;
      }
      .actions {
        flex-direction: column;
      }
    }
  </style>

</head>
<body>

<!-- Navbar -->
<nav class="navbar">
  <div class="logo">
    <img src="https://i.ibb.co/sdXpQ0tv/Whats-App-Image-2025-09-19-at-23-46-25-4ba03904.jpg" alt="Odisha Logo">
    Odisha Tourism
  </div>

  <ul class="nav-links" id="navLinks">
    <li><a href="/" class="active"><i class="fas fa-home"></i> Home</a></li>
    <li><a href="/places"><i class="fas fa-map-marked-alt"></i> Tourist Places</a></li>
    <li><a href="/contact"><i class="fas fa-envelope"></i> Contact</a></li>
  </ul>

  <div style="display:flex; align-items:center; gap:15px;">
    <!-- Hamburger menu icon -->
    <div class="hamburger" id="hamburger">
      <i class="fas fa-bars"></i>
    </div>
  </div>
</nav>


  <!-- Contact Section -->
  <section class="contact-section">
    <div class="contact-card">
      <h2>Contact Us</h2>
      <p class="muted">We’d love to hear from you! Fill out the form below.</p>

      <form id="contactForm" class="contact-form" action="/contact" method="POST">
        <div class="row">
          <div class="field">
            <label for="name">Name</label>
            <input type="text" id="name" name="name" required>
          </div>
          <div class="field">
            <label for="email">Email</label>
            <input type="email" id="email" name="email" required>
          </div>
        </div>
        <div class="field">
          <label for="subject">Subject</label>
          <input type="text" id="subject" name="subject">
        </div>
        <div class="field">
          <label for="message">Message</label>
          <textarea id="message" name="message" rows="5" required></textarea>
        </div>
        <div class="actions">
          <button type="submit" class="btn primary">Send</button>
          <button type="reset" class="btn ghost">Reset</button>
        </div>
        <div id="formFeedback" class="form-feedback"></div>
      </form>
    </div>

    <aside class="contact-info">
      <div class="info-block">
        <h3>Office</h3>
        <p>Bhubaneswar, Odisha</p>
      </div>
      <div class="info-block">
        <h3>Email</h3>
        <p>bhagyabratagantayat@gmail.com</p>
      </div>
      <div class="info-block">
        <h3>Phone</h3>
        <p>+91 78550 91725</p>
      </div>
    </aside>
  </section>

  <!-- Booking Section -->
  <section class="booking-section">
    <h2>Book Your Trip</h2>
    <p>Plan your journey with us — fill out the booking form below.</p>

    <form class="booking-form" action="/booking" method="POST">
      <div class="row">
        <div class="field">
          <label for="fullname">Full Name</label>
          <input type="text" id="fullname" name="fullname" required>
        </div>
        <div class="field">
          <label for="phone">Phone</label>
          <input type="tel" id="phone" name="phone" required>
        </div>
      </div>

      <div class="row">
        <div class="field">
          <label for="destination">Destination</label>
          <select id="destination" name="destination" required>
            <option value="">-- Select Place --</option>
            <option>Puri Jagannath Temple</option>
            <option>Konark Sun Temple</option>
            <option>Chilika Lake</option>
            <option>Lingaraj Temple</option>
            <option>Dhauli Shanti Stupa</option>
            <option>Udayagiri and Khandagiri Caves</option>
            <option>Nandankanan Zoological Park</option>
            <option>Hirakud Dam</option>
            <option>Similipal National Park</option>
            <option>Satkosia Gorge</option>
            <option>Rajarani Temple</option>
            <option>Ananta Vasudeva Temple</option>
            <option>Mukteswara Temple</option>
            <option>Khandadhar Waterfall</option>
            <option>Bhitarkanika National Park</option>
            <option>Chandipur Beach</option>
            <option>Gopalpur Beach</option>
            <option>Balasore Panchalingeswar Temple</option>
            <option>Taratarini Temple</option>
            <option>Biraja Temple, Jajpur</option>
            <option>Samaleswari Temple, Sambalpur</option>
            <option>Gupteswar Cave Temple</option>
            <option>Mahendragiri Hill</option>
            <option>Deomali Hill</option>
          </select>
        </div>
        <div class="field">
          <label for="travelDate">Travel Date</label>
          <input type="date" id="travelDate" name="travelDate" required>
        </div>
      </div>

      <div class="field">
        <label for="people">Number of People</label>
        <input type="number" id="people" name="people" min="1" required>
      </div>

      <div class="field">
        <label for="notes">Additional Notes</label>
        <textarea id="notes" name="notes" rows="4"></textarea>
      </div>

      <button type="submit">Confirm Booking</button>
    </form>
  </section>
</body>
</html>
